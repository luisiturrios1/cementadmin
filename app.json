{
  "name": "cementadmin",
  "description": "Aplicación web Django para administrar cementerios",
  "image": "python:2.7",
  "repository": "https://github.com/luisiturrios1/cementadmin",
  "keywords": ["python", "django" ],
  "addons": [ "heroku-postgresql" ],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "python manage.py collectstatic --noinput",
        "test": "python manage.py test"
      }
    }
  }
}
